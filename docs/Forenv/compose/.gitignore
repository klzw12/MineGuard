# Docker Compose 配置文件
# 排除包含敏感信息的配置文件

# 正式环境配置文件（包含敏感信息）
docker-compose-prod.yml

# Redis 配置文件（包含密码等敏感信息）
redis.conf

# 综合配置文件（冗余，已分离为prod和test）
docker-compose-all.yml

# Redis专用配置文件（冗余，已包含在prod和test配置中）
docker-compose-redis.yml