server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mineguard?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
  data:
    redis:
      host: localhost
      port: 6379
      password:

logging:
  level:
    com.klzw: debug
